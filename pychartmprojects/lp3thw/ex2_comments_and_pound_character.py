print("# Isn't this should have been a comment ?")

# The # in that code is inside a string,
# so it will be put into the string until the ending " character is hit.
# Pound characters in strings are just considered characters, not comments.